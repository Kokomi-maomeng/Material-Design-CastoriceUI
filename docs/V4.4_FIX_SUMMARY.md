# v4.4 requested work and verification

| Request | Implementation |
| --- | --- |
| Sustained press and Material motion | Shared press state layer on buttons, navigation, segmented controls, setup entries, and service links; reduced-motion override. |
| CPU/memory current and history | Current values on Services; bounded SQL aggregations from existing one-minute samples for 1h/6h/24h; cached per persisted sample. |
| Five-second chart refresh | Persistent chart error boundary and position-based pointer selection across new samples. |
| Traffic secondary cards | Wider monthly card, smaller donut and legend, balanced desktop heights, narrow-screen single column. |
| Information popovers | Trigger-based placement, viewport clamping, resize/scroll repositioning, wrapping content. |
| Setup coverage | First-run and recurring setup pages explain protected configuration prerequisites and real-sample warm-up. Existing integration validation remains server-backed. |
| Overview service health | One dynamic compact card with per-item navigation. |
| Overview chart whitespace | Flexible chart fills the height adjacent to connection and network cards. |
| English | New copy has paired Chinese/English strings; browser and runtime verification recorded separately. |
| Release naming | Package, lockfile, README, deployment guide, integration guide, archive manifest, and application version resolve to 4.4.0. |

## Local verification (2026-09-24)

- `npm run check`: 32 Node contracts, 29 UI runtime tests, 102 backend tests, sensitive-content scan, production build, and zero production dependency-audit findings.
- `npm audit --audit-level=low`: zero findings across the complete installed dependency tree.
- Playwright Chromium and WebKit: 100 layout cases each, covering ten pages in Chinese and English at 320, 390, 768, 1440, and 2560 px; no JavaScript errors. The matrix also checks five-second chart-hover persistence, secondary-card heights, anchored popover viewport bounds, and traffic quota interaction.
- Local Firefox launch failed on Windows before tests started (`BrowserType.launch: spawn UNKNOWN`); the Linux CI browser matrix remains the Firefox acceptance gate.
- Gitleaks working-tree and Git-history scans passed. The archive and post-merge repository require separate publication scans.

Static and synthetic-browser checks do not prove production protocol connectivity or service continuity. The deployment record and final GitHub checks must use live results.
